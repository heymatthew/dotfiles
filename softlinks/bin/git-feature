#!/usr/bin/env bash

DESC=$1

if [ -z "$DESC" ]; then
  # List branches, newest first
  git branch --format "%(refname:short)" --sort=-authordate
  exit 0
fi

git fetch
git checkout "origin/HEAD" -b "$DESC"
