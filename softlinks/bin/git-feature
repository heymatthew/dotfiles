#!/usr/bin/env bash

DESC=$1

if [ -z "$DESC" ]; then
  # If we're not creating a branch, then list branches
  git branch --format "%(refname:short)"
  exit 0
fi

git fetch
git checkout "origin/HEAD" -b "$DESC"
