#!/usr/bin/env bash

NAME=$1

if [ -z "$NAME" ]; then
  # If we're not creating a branch, then list branches
  git branch --format "%(refname:short)"
  exit 0
fi

git checkout -b "$NAME" "origin/HEAD"
