#!/usr/bin/env bash

MAINLINE="origin/HEAD"
DESC=$1

if [ -z "$DESC" ]; then
  # List features
  git branch --format "%(refname:short)" | grep '^feature/'

  exit 0
fi

git fetch
git checkout "$MAINLINE" -b "feature/$DESC"
