theme = dark:yui-dark,light:yui-light
scrollback-limit = 20000000

# https://ghostty.org/docs/config/reference#font-feature
font-feature = -calt
font-feature = -liga
font-feature = -dlig
font-family = M+ 1mn Medium
font-size = 14

# I install this as a cask through brew, no need to prompt
auto-update = off
