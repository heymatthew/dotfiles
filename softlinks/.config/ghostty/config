theme = dark:yui-dark,light:yui-light
scrollback-limit = 20000000

# https://ghostty.org/docs/config/reference#font-feature
font-feature = -calt
font-feature = -liga
font-feature = -dlig
font-family = Iosevka Medium
font-size = 14

# Text in unfocused splits are legible
unfocused-split-opacity = 1

# I install this as a cask through brew, no need to prompt
auto-update = off

keybind = super+shift+f=write_selection_file:open
keybind = cmd+f=write_screen_file:open

# I manage my CWD pin and cdd so I don't need this
window-inherit-working-directory = false